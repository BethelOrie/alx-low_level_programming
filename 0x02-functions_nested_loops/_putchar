#include <unistd.h>

/**
* _putchar -writes the haracter c to stdout
*@c: The character to print
*Return: on success 1
*On erreor, -1 is returned, and errno is set appropraitely
*/
int _putchar(char c)
*include <unistd.h>
*
/**
*_putchar - writes the characterb c to stdout
*@c:The character to print 
*Return : on success 1.
*On error , -1 is returned. and error is set appropraitely
/*
int _putchar(char letter);

{
 return(write(1, &c, 1));
}
