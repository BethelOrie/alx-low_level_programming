#include <unistd.h>
#/**
# _putchar the- writes the character c to stdout
# @c: The character to print
#
# retturn: on success 1.
# on error, -1 is returned, and erno is set appropraitely
#/*
# int _putchar(char c)
{
 return (write(1, &c, 1));
}
