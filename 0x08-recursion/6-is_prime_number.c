#include "main.h"

/**
*int is_prime_number -evaluates prime or not
*@n: Number
*Return: return 1 prime - return 0 otherwise
*/
int is_prime_number(int n)
{

int n
n = 2;

/* only greater than 2*/
if (n < 2)
{
return (0);
}

if (n == 2)
{
return (1);
}
return ('\n');
}
